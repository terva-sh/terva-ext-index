{
  "name": "index",
  "version": "0.8.2",
  "exec": "./run.sh",
  "language": "rust",
  "description": "Emit a file's tree-sitter skeleton (imports + signatures with exact line ranges) so the model can understand structure before a full read.",
  "data_secrets": false,
  "enabled": true
}
